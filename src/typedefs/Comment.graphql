type Comment {
  id: ID!
  comment: String!
  review: Int
  author: Author!
}

input CommentInput {
  comment: String!
  review: Int
  productId: ID!
  authorId: ID!
}

union AddCommentResponse = Comment | Error

type Error {
  message: String!
}